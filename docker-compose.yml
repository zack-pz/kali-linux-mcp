services:
  security-tools:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_HOST=database
      - DB_USER=admin
      - DB_PASSWORD=password
      - DB_NAME=auth-service
    depends_on:
      database:
        condition: service_healthy
    restart: unless-stopped